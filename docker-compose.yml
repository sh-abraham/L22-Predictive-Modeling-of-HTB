version: "3"

services:
  app:
    build:
      context: "./Frontend/Docker"
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./Frontend:/app/frontend
    command: bash entrypoint
